---
interface Props {
	date: Date;
	showDay?: boolean
}

const { date, showDay = true } = Astro.props;
---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString('en-us', {
			year: 'numeric',
			month: 'short',
			day: showDay ? 'numeric' : undefined,
		})
	}
</time>
